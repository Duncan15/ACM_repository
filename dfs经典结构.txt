dfs()
{
    if条件判断——（到终点） 
    {
        操作……
        return;
    }
    if……省略一些条件判断（剪枝优化，比如一些没必要的continue或者计算过的子问题的标记备忘录直接存取）
    for()
        if……满足条件的筛选
        修改标记；
        dfs();
        改回标记；
}
